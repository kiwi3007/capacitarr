# Capacitarr 2.0 — Phased Execution Plan

> **Status:** 🔄 In Progress — Phase 1 ✅, Phase 2 ✅, Phase 3 ✅, Phase 4 ✅, Phase 5 ✅, Phase 6 ✅, Phase 7 ✅, Phase 8 ✅
> **Created:** 2026-03-18
> **Design Reference:** [`20260318T2119Z-capacitarr-2.0-plan.md`](20260318T2119Z-capacitarr-2.0-plan.md)

This document is the actionable execution plan for the Capacitarr 2.0 release. All design decisions, rationale, and feature deep dives live in the [original plan](20260318T2119Z-capacitarr-2.0-plan.md). This plan focuses on **what to build, in what order, with what acceptance criteria**.

---

## Table of Contents

- [Branch Strategy](#branch-strategy)
- [Coding Rules & Guidelines](#coding-rules--guidelines)
- [Phase 1: Fresh Schema Baseline](#phase-1-fresh-schema-baseline)
- [Phase 2: Backend Architectural Refactors](#phase-2-backend-architectural-refactors)
- [Phase 3: Frontend Stack Changes](#phase-3-frontend-stack-changes)
- [Phase 4: Frontend Restructuring](#phase-4-frontend-restructuring)
- [Phase 5: Features — Backend APIs](#phase-5-features--backend-apis)
- [Phase 6: Features — Frontend UI](#phase-6-features--frontend-ui)
- [Phase 7: Migration Script](#phase-7-migration-script)
- [Phase 8: Polish, Testing & Release](#phase-8-polish-testing--release)
- [Phase 9: Code Audit & Deep Polish](#phase-9-code-audit--deep-polish)
- [Appendix A: Feature Deep Dives](#appendix-a-feature-deep-dives)
- [Appendix B: Effort Estimates](#appendix-b-effort-estimates)

---

## Branch Strategy

Create `feature/2.0` from `main`. All 2.0 work happens on this long-lived branch. When complete, merge to `main` and tag `v2.0.0`. See the [original plan — Branch Strategy](20260318T2119Z-capacitarr-2.0-plan.md#branch-strategy) for the full branching diagram.

**Pre-flight checklist:**

- [ ] All in-progress 1.x feature branches merged to `main`
- [ ] `main` is green (CI passing)
- [ ] `feature/2.0` created from `main`

---

## Coding Rules & Guidelines

These rules apply to **all phases** of 2.0 development. They supplement the project-wide rules in `.kilocoderules`.

### Execution Discipline

1. **Complete all steps within a phase before moving to the next phase.** Do not cherry-pick easy steps from later phases while skipping harder steps in the current phase.
2. **Never silently defer steps.** If a step cannot be completed as written, stop and ask the user for guidance — do not mark it "deferred" or "TODO later" and move on.

### Backend Rules

1. **Service layer architecture is mandatory.** All DB access goes through services. No exceptions. See `.kilocoderules` — "Service Layer Architecture" section.
2. **Every new service** must accept `*gorm.DB` and `*events.EventBus`, be registered on `services.Registry`, and include unit tests using the in-memory SQLite pattern.
3. **New integration code** must implement capability interfaces (see Phase 2), not the old monolithic `Integration` interface.
4. **All new scoring factors** must implement `ScoringFactor` interface (see Phase 2). No hardcoded scoring logic in the evaluator loop.
5. **All new enrichers** must implement the `Enricher` interface (see Phase 2). No additions to a monolithic `EnrichItems()` function.
6. **Conventional Commits** for every commit. Format: `<type>(<scope>): <description>`.
7. **`make ci` must pass** before any commit is declared ready.
8. **Canonical test names:** Use "Firefly" for TV shows, "Serenity" for movies in all test data.
9. **No `!important` in CSS.** Fix the root cause. No `eslint-disable` or `@ts-ignore`. No warning suppression.
10. **Use shadcn-vue components** everywhere. No raw `<input>`, `<select>`, `<table>`, etc.

### Frontend Rules

1. **ECharts** for all new charts (Phase 3 migrates existing ApexCharts). Tree-shake to keep bundle size reasonable.
2. **`@tanstack/vue-virtual`** for any list that could exceed ~50 items.
3. **`DashboardCard`** component for every analytics card — no one-off card wrappers.
4. **Granular SSE subscriptions** — components subscribe to specific event types, not `*`.
5. **Query parameter pattern** for cross-page linking: `/library?filter=dead`, `/library?quality=720p`, `/library?ruleId=5`.
6. **i18n** — every user-visible string must use the i18n system, even for new features. Add keys to all 22 language files (English values as placeholder for untranslated strings is acceptable).

### Schema Rules

1. **No incremental migrations from 1.x.** The 2.0 schema is a clean baseline.
2. **All new tables and columns** follow the naming conventions established in the baseline migration.
3. **Foreign keys** are required for all relationships. Cascading deletes where appropriate.
4. **No nullable columns without a documented reason.** Prefer `NOT NULL` with defaults.

---

## Phase 1: Fresh Schema Baseline

**Goal:** Replace the 11 incremental migration files with a single new baseline that reflects the 2.0 domain model.

**Design reference:** [Original plan — Fresh Schema Baseline](20260318T2119Z-capacitarr-2.0-plan.md#fresh-schema-baseline)

### Steps

1. **Design the 2.0 schema.** Document all tables, columns, types, constraints, and relationships in a schema design doc or directly in the migration SQL file with comments. Key new entities:
   - `libraries` — first-class library entity (see [Library Abstraction](20260318T2119Z-capacitarr-2.0-plan.md#7-library-abstraction))
   - Renamed integration type: `overseerr` → `seerr` (see [Overseerr → Seerr Rename](20260318T2119Z-capacitarr-2.0-plan.md#overseerr--seerr-rename))
   - Nullable `threshold_pct` / `target_pct` on `integration_configs` for per-integration thresholds
   - Pluggable scoring factor weight columns
2. **Delete existing migration files** (`00001_baseline.sql` through `00011_add_cancelled_audit_action.sql`).
3. **Create `00001_v2_baseline.sql`** — single comprehensive migration file.
4. **Update `db/models.go`** to match the new schema. All GORM model structs must align with the baseline SQL.
5. **Update `db/migrate.go`** to reference only the new baseline.
6. **Update `db/validation.go`** if any validation logic needs adjustment for new/changed columns.
7. **Run `make ci`** — all existing tests will break here; that's expected. Fix model-level test compilation errors only (integration-level test fixes happen in later phases).

### Acceptance Criteria

- [x] Single `00001_v2_baseline.sql` migration file exists
- [x] All old migration files removed
- [x] `db/models.go` struct definitions match the baseline schema exactly
- [x] `libraries` table present with `id`, `name`, `disk_group_id`, `threshold_pct`, `target_pct` columns
- [x] `integration_configs.type` accepts `"seerr"` (no `"overseerr"`)
- [x] `integration_configs` has nullable `threshold_pct` and `target_pct` columns
- [x] Schema design is documented (either inline SQL comments or a separate doc)
- [x] `make ci` passes (model-level tests compile and pass)

> **Note:** `db/migrate.go` did not need changes — it uses `embed.FS` to discover migration files by glob, so the renamed file is picked up automatically. Also, `db/db.go` was updated to seed the new preference fields (`RequestPopularityWeight`, `QualityBloatWeight`, `DeadContentMinDays`, `StaleContentDays`). All existing tests pass without modification because the schema changes are additive (new tables, new nullable columns on existing tables).

---

## Phase 2: Backend Architectural Refactors

**Goal:** Restructure the backend internals to use capability-based interfaces, a composable enrichment pipeline, pluggable scoring factors, and an extracted evaluator. This phase is pure refactoring — no new features, no new API endpoints.

**Design reference:** [Original plan — Architectural Refactors](20260318T2119Z-capacitarr-2.0-plan.md#architectural-refactors)

### Steps

1. **Define capability interfaces** in `internal/integrations/types.go`:
   - `Connectable`, `MediaSource`, `DiskReporter`, `MediaDeleter`, `WatchDataProvider`, `RequestProvider`, `WatchlistProvider`
   - See [original plan — §1 Capability-Based Integration Interfaces](20260318T2119Z-capacitarr-2.0-plan.md#1-capability-based-integration-interfaces) for exact signatures
2. **Refactor each integration client** to implement only the interfaces it supports:
   - Rename `overseerr.go` → `seerr.go`, `OverseerrClient` → `SeerrClient`, constant `IntegrationTypeOverseerr` → `IntegrationTypeSeerr`
   - Sonarr: `Connectable + MediaSource + DiskReporter + MediaDeleter`
   - Radarr: same as Sonarr
   - Readarr, Lidarr: same pattern, scope per actual capabilities
   - Plex: `Connectable + WatchDataProvider + WatchlistProvider`
   - Tautulli: `Connectable + WatchDataProvider`
   - Seerr: `Connectable + RequestProvider`
   - Jellyfin: `Connectable + WatchDataProvider + WatchlistProvider`
   - Emby: `Connectable + WatchDataProvider + WatchlistProvider`
3. **Build the `IntegrationRegistry`** in `internal/integrations/registry.go`:
   - Stores registrations keyed by integration ID
   - Methods: `WatchProviders()`, `MediaSources()`, `DiskReporters()`, `RequestProviders()`, `Deleters()`, etc.
   - See [original plan — §2 Registry-Based Integration Discovery](20260318T2119Z-capacitarr-2.0-plan.md#2-registry-based-integration-discovery)
4. **Implement plugin-style registration** with factory functions:
   - `Register("sonarr", SonarrFactory{})` pattern in `init()` or an explicit `RegisterAll()` call
   - See [original plan — Plugin-Style Integration Registration](20260318T2119Z-capacitarr-2.0-plan.md#plugin-style-integration-registration)
5. **Refactor the `MediaItem` model** to the layered structure:
   - Core `MediaItem` + `EnrichmentData` + typed `MediaMetadata` interface (with `TVMetadata`, `MusicMetadata`, etc.)
   - See [original plan — §3 Layered Media Model](20260318T2119Z-capacitarr-2.0-plan.md#3-layered-media-model)
6. **Build the pluggable enrichment pipeline** in `internal/integrations/enrichment.go` (or a new `internal/enrichment/` package):
   - `Enricher` interface with `Name()`, `Priority()`, `Enrich(items []MediaItem) error`
   - `EnrichmentPipeline` struct with `Run(items []MediaItem)`
   - Migrate existing enrichment logic into individual enricher implementations
   - See [original plan — §4 Pluggable Enrichment Pipeline](20260318T2119Z-capacitarr-2.0-plan.md#4-pluggable-enrichment-pipeline)
7. **Build pluggable scoring factors** in `internal/engine/`:
   - `ScoringFactor` interface with `Name()`, `Key()`, `DefaultWeight()`, `Calculate(item MediaItem) float64`
   - Migrate existing scoring dimensions into individual `ScoringFactor` implementations
   - Register default factors slice
   - See [original plan — §5 Pluggable Scoring Factors](20260318T2119Z-capacitarr-2.0-plan.md#5-pluggable-scoring-factors)
8. **Extract the evaluator from the poller**:
   - `internal/engine/evaluator.go` — reusable scoring + filtering logic
   - Poller becomes an orchestrator calling the evaluator
   - Action Router dispatches results to delete, approval queue, dry-run, or analytics
   - See [original plan — §6 Separate Evaluation from Action](20260318T2119Z-capacitarr-2.0-plan.md#6-separate-evaluation-from-action)
9. **Create the Library abstraction** — `LibraryService` in `internal/services/`:
   - CRUD for `Library` entity (grouping integrations with per-library thresholds)
   - `EffectiveThresholdForIntegration(integrationID)` method
   - See [original plan — §7 Library Abstraction](20260318T2119Z-capacitarr-2.0-plan.md#7-library-abstraction)
10. **Update the poller** (`internal/poller/`) to use the new registry, enrichment pipeline, and evaluator instead of the old manual wiring.
11. **Update all route handlers** to use the refactored services and models. No new endpoints — just update existing ones to compile with the new types.
12. **Update all existing tests** to work with the refactored architecture. Fix compilation errors, update test helpers, adjust assertions.
13. **Run `make ci`** — everything must pass.

### Acceptance Criteria

- [x] Capability interfaces defined and implemented by all integration clients
- [x] `IntegrationRegistry` replaces `EnrichmentClients` and switch-statement wiring
- [x] `overseerr.go` → `seerr.go` rename complete (code, types, constants, tests)
- [x] `MediaItem` uses layered model (`EnrichmentData`, typed `MediaMetadata`)
- [x] `EnrichmentPipeline` with individual `Enricher` implementations replaces monolithic `EnrichItems()`
- [x] `ScoringFactor` interface with individual factor implementations replaces hardcoded scoring loop
- [x] Evaluator extracted from poller; poller is now an orchestrator
- [x] `LibraryService` exists with CRUD and `EffectiveThresholdForIntegration()`
- [x] All existing tests pass with refactored architecture
- [x] `make ci` passes
- [x] No new API endpoints or features — behavior is identical to pre-refactor

> **Note:** Steps 1–9 (interfaces, registry, factory, enrichment pipeline skeleton, scoring factors, evaluator, library service) were completed in earlier commits. Steps 10–13 completed the remaining ~10%:
> - **Step 10:** Rewrote `fetch.go` to use `IntegrationRegistry` + `EnrichmentPipeline` instead of `EnrichmentClients` + `EnrichItems()` + `NewClient()`. Built concrete `Enricher` implementations: `BulkWatchEnricher`, `TautulliEnricher`, `RequestEnricher`, `WatchlistEnricher`, `CrossReferenceEnricher` in `enrichers.go`. `BuildEnrichmentPipeline()` auto-discovers enrichers from registry capabilities.
> - **Step 10 (evaluate):** Rewrote `evaluate.go` to use the extracted `Evaluator` and get `MediaDeleter` from registry instead of `serviceClients map[uint]Integration`.
> - **Step 10 (poller):** Updated `poller.go` to run `pipeline.Run()` instead of `integrations.EnrichItems()`.
> - **Step 11:** Changed `DeleteJob.Client` from `Integration` to `MediaDeleter`. Updated `preview.go` to use `BuildIntegrationRegistry()`, `approval.go` to use `CreateClient()` + `MediaDeleter` type assertion, `integration.go` `SyncAll()` and `TestConnection()` to use factory + capability interfaces.
> - **Client compliance:** Added no-arg `GetBulkWatchData()` wrappers to Jellyfin and Emby (resolving admin user internally). Added compile-time interface checks for Plex (`WatchDataProvider`, `MediaSource`), Jellyfin/Emby (`WatchDataProvider`), Tautulli (`Connectable`).
> - **Tests:** Updated `fetch_test.go` for new `fetchResult` shape. Added `RegisterAllFactories()` to `testutil.SetupTestServerWithRegistry()`.
> - **Lint fixes:** Fixed pre-existing `migration/migrate.go` lint issues (blank import comment, `sourceSqlDB` → `sourceSQLDB`). Formatted pre-existing frontend prettier issues.

---

## Phase 3: Frontend Stack Changes

**Goal:** Swap out charting library, build reusable components, and set up infrastructure patterns before touching page layouts.

**Design reference:** [Original plan — Frontend Stack Decisions](20260318T2119Z-capacitarr-2.0-plan.md#frontend-stack-decisions)

### Steps

1. **Replace ApexCharts with ECharts:**
   - Remove `apexcharts` + `vue3-apexcharts` dependencies
   - Add `echarts` + `vue-echarts` dependencies
   - Create a tree-shakeable ECharts setup file (import only required chart types, renderers, components)
   - Migrate any existing chart components to ECharts equivalents
   - Verify dark mode integration with the existing theme system
2. **Build the `CreatableCombobox` component:**
   - Extends the shadcn-vue Combobox recipe on top of Reka-UI primitives
   - Feature: synthetic "Create: {typed value}" option when no match
   - Feature: `modelValue` is authoritative regardless of suggestion list presence (fixes prepopulation)
   - Path: `frontend/src/components/ui/creatable-combobox/CreatableCombobox.vue`
   - See [original plan — Change 2](20260318T2119Z-capacitarr-2.0-plan.md#change-2-build-a-creatable-combobox-on-reka-ui-primitives)
3. **Build the `DashboardCard` component:**
   - Slots: Header (title + action buttons), Content (chart/stat grid/table), Footer (optional summary/link)
   - Path: `frontend/src/components/ui/dashboard-card/DashboardCard.vue`
   - See [original plan — Change 4](20260318T2119Z-capacitarr-2.0-plan.md#change-4-dashboard-card-composition-pattern)
4. **Apply virtual scrolling to all unbounded lists:**
   - Library page media list
   - Approval queue
   - Audit log
   - Activity feed
   - Rule builder dropdowns
   - See [original plan — Change 3](20260318T2119Z-capacitarr-2.0-plan.md#change-3-virtual-scrolling-everywhere)
5. **Implement granular SSE event subscriptions:**
   - Update the SSE composable to filter by event type before invoking callbacks
   - Components subscribe to specific event types (not all 44+)
   - Pages that don't need real-time updates don't process events
   - See [original plan — Change 5](20260318T2119Z-capacitarr-2.0-plan.md#change-5-granular-sse-event-subscriptions)
6. **Run frontend linting and build** — verify no regressions.

### Acceptance Criteria

- [x] ApexCharts fully removed from `package.json` and codebase
- [x] ECharts renders at least one test chart in dark mode correctly
- [x] `CreatableCombobox` supports creation of custom values and prepopulation of existing values
- [x] `DashboardCard` component exists with header/content/footer slots
- [x] Virtual scrolling applied to Library, Approval, Audit, Activity, and dropdown lists
- [x] SSE composable supports per-event-type subscriptions
- [x] Frontend builds without errors or warnings
- [x] `make ci` passes

---

## Phase 4: Frontend Restructuring

**Goal:** Reorganize navigation, build the new page layouts, and implement the universal bottom toolbar. No new backend APIs yet — pages may use placeholder/mock data.

**Design reference:** [Original plan — Frontend Restructuring](20260318T2119Z-capacitarr-2.0-plan.md#frontend-restructuring)

### Steps

1. **Build the universal bottom toolbar:**
   - Fixed bottom bar with glassmorphism styling
   - Relocate: engine control, update check, language selector, theme picker, donate, help, logout
   - All popovers/dropdowns open upward (`side="top"`)
   - iOS safe area support (`viewport-fit=cover` + `env(safe-area-inset-bottom)`)
   - Adjust toast container offset to clear the toolbar
   - Simplify top navbar to: brand (left) + page navigation links (right)
   - See [original plan — Universal Bottom Toolbar](20260318T2119Z-capacitarr-2.0-plan.md#universal-bottom-toolbar)
2. **Update navigation to the new 6-item structure:**
   - `Dashboard · Insights · Library · Rules · Settings · Help`
   - Rename "Audit Log" to be within Library → History tab
   - Add "Insights" nav item pointing to `/insights`
3. **Create the Insights page** (`/insights`) with three-tab skeleton:
   - Tab 1: Overview (placeholder cards)
   - Tab 2: Quality (placeholder cards)
   - Tab 3: Watch Intelligence (placeholder cards with "configure a media server" empty state)
   - See [original plan — Insights page](20260318T2119Z-capacitarr-2.0-plan.md#insights--new-page-insights)
4. **Restructure the Library page** (`/library`) to two-tab layout:
   - Tab 1: Browse (existing media browser + smart filter presets UI — Dead, Stale, Bloated, Requested, Protected)
   - Tab 2: History (merge existing Audit Log page here)
   - Smart filter query params: `/library?filter=dead`, etc.
   - See [original plan — Library page](20260318T2119Z-capacitarr-2.0-plan.md#library--enhanced-library)
5. **Update the Rules page** layout scaffolding:
   - In-line editing UI using `CreatableCombobox` (from Phase 3)
   - Rule impact badge placeholders
   - Drag-and-drop sort order (use existing `@vueuse/core` utilities or a lightweight sortable)
   - See [original plan — Rules page](20260318T2119Z-capacitarr-2.0-plan.md#rules--enhanced-rules)
6. **Update the Settings page** with per-integration threshold UI:
   - "Override Thresholds" toggle on each integration card
   - Threshold % and target % sliders (hidden until toggle enabled)
   - See [original plan — Settings page](20260318T2119Z-capacitarr-2.0-plan.md#settings--enhanced-settings)
7. **Implement cross-page linking pattern:**
   - Insights → Library links (chart segment clicks → query params)
   - Rules → Library links (rule impact badge → `/library?ruleId=N`)
   - Library filter bar showing active filter context
   - See [original plan — Cross-Page Linking](20260318T2119Z-capacitarr-2.0-plan.md#cross-page-linking-pattern)
8. **Add i18n keys** for all new UI strings across all 22 language files.
9. **Visual QA** — verify all pages render correctly in light and dark modes, desktop and mobile viewports.

### Acceptance Criteria

- [x] Bottom toolbar visible on all viewports with all utility icons relocated
- [x] Top navbar shows only brand + 6 page navigation links
- [x] Insights page loads with three tabs and placeholder `DashboardCard` components
- [x] Library page has Browse + History tabs; smart filter presets render with correct icons
- [x] Audit Log is no longer a standalone page (merged into Library → History)
- [x] Rules page has CreatableCombobox in rule editing, drag-and-drop sort, and impact badge placeholders
- [x] Settings integration cards show threshold override toggles
- [x] Cross-page links navigate correctly with query parameters
- [x] All new strings are in i18n system
- [x] Dark mode + mobile viewport rendering verified
- [x] `make ci` passes

> **Note:** Phase 4 completion details:
> - **Step 1 (Bottom toolbar):** Created `BottomToolbar.vue` with glassmorphism (`backdrop-blur-xl`, `bg-background/70`), iOS safe area support (`env(safe-area-inset-bottom)`). Relocated engine control, update check, language selector, theme picker, donate, and logout from Navbar. All popovers/dropdowns open upward (`side="top"`). Added `side` prop to `EngineControlPopover`. Adjusted `ToastContainer` offset to `bottom-16` to clear the toolbar. Added `pb-20` to main content when authenticated.
> - **Step 2 (Navigation):** Simplified Navbar to brand (left) + 6 nav links (right): Dashboard · Insights · Library · Rules · Settings · Help. Removed all utility icons (moved to bottom toolbar). Added `nav.rules` i18n key.
> - **Step 3 (Insights):** Already complete from Phase 3 work — 3 tabs (Overview with ECharts, Quality placeholders, Watch Intelligence placeholders with Library links).
> - **Step 4 (Library):** Restructured to two-tab layout (Browse + History). Added smart filter presets (Dead, Stale, Bloated, Requested, Protected) with icons and query param routing. Extracted audit log into reusable `AuditLogPanel.vue` component. Existing `audit.vue` page redirects to `/library?tab=history`.
> - **Step 5 (Rules):** Replaced `UiCombobox` with `CreatableCombobox` in `RuleBuilder.vue` for free-text value input. Added rule impact badge placeholders (linking to `/library?ruleId=N`) in `RuleCustomList.vue`. Drag-and-drop already existed.
> - **Step 6 (Settings thresholds):** Added per-integration "Override Thresholds" toggle with threshold % and target % sliders to `SettingsIntegrations.vue`. Added `thresholdPct` and `targetPct` fields to `IntegrationConfig` TypeScript type.
> - **Step 7 (Cross-page linking):** Insights → Library links via `/library?filter=dead` and `/library?filter=stale`. Rules → Library via `/library?ruleId=N` impact badges. Library reads `filter` and `ruleId` query params. Library filter bar shows active filter name and clear button.
> - **Step 8 (i18n):** Added 17 new keys to all 22 locale files (English values as placeholders for non-English locales).
> - **Step 9 (QA):** `make ci` passes — lint (golangci-lint, ESLint, Prettier, TypeScript), tests (Go + Vitest), and security (govulncheck, pnpm audit, Trivy, Gitleaks, Semgrep) all clean.

---

## Phase 5: Features — Backend APIs

**Goal:** Build the backend services and API endpoints that power the 8 cherry-picked features. The evaluator, enrichment pipeline, and registry from Phase 2 make this mostly "write aggregation logic and expose via endpoints."

**Design reference:** [Original plan — Selected Features](20260318T2119Z-capacitarr-2.0-plan.md#selected-features-8) and [Feature Deep Dives](#appendix-a-feature-deep-dives)

### Steps

1. **Create `AnalyticsService`** in `internal/services/analytics.go`:
   - `GetComposition(libraryID)` — quality, genre, year, type, integration aggregations
   - `GetGrowthTrend(libraryID, period)` — growth over time from historical rollups
   - `GetQualityDistribution(libraryID)` — detailed quality breakdown
   - `GetSizeAnomalies(libraryID)` — bloat detection (items > 2x median for quality profile)
   - Data source: preview cache (existing `MediaItem` slice). No new DB tables for aggregations.
2. **Create `WatchAnalyticsService`** in `internal/services/watch_analytics.go`:
   - `GetDeadContent(libraryID, minAgeDays)` — PlayCount == 0, not on watchlist, added > N days ago
   - `GetStaleContent(libraryID, staleDays)` — LastPlayed > N days, PlayCount > 0, with staleness score
   - `GetPopularity(libraryID)` — heatmap data (genre × year → play count), ranked lists
   - `GetRequestFulfillment(libraryID)` — fulfillment rate, unfulfilled list, per-user breakdown
   - Requires enrichment data; items without enrichment excluded to avoid false positives.
3. **Add API routes:**
   - `GET /api/v1/analytics/composition` → `AnalyticsService.GetComposition`
   - `GET /api/v1/analytics/growth` → `AnalyticsService.GetGrowthTrend`
   - `GET /api/v1/analytics/quality` → `AnalyticsService.GetQualityDistribution`
   - `GET /api/v1/analytics/bloat` → `AnalyticsService.GetSizeAnomalies`
   - `GET /api/v1/analytics/dead-content` → `WatchAnalyticsService.GetDeadContent`
   - `GET /api/v1/analytics/stale-content` → `WatchAnalyticsService.GetStaleContent`
   - `GET /api/v1/analytics/popularity` → `WatchAnalyticsService.GetPopularity`
   - `GET /api/v1/analytics/request-fulfillment` → `WatchAnalyticsService.GetRequestFulfillment`
4. **Add per-integration threshold support to `IntegrationService`:**
   - `UpdateThresholds(integrationID, thresholdPct, targetPct)` method
   - Update `DiskGroupService` to support `EffectiveThresholdForIntegration()`
5. **Add rule impact preview to `RuleService`:**
   - `GetRuleImpact(ruleID)` → count of items affected by this rule
   - Uses the evaluator from Phase 2 to test-run the rule against the preview cache
6. **Add `LibraryService` API routes** (if not already exposed in Phase 2):
   - `GET /api/v1/libraries` — list all libraries
   - `POST /api/v1/libraries` — create library
   - `PUT /api/v1/libraries/:id` — update library
   - `DELETE /api/v1/libraries/:id` — delete library
7. **Publish SSE events** for analytics cache invalidation:
   - `analytics_updated` event when preview cache refreshes
   - Frontend uses this to refetch analytics data
8. **Write tests** for all new services and route handlers:
   - Unit tests for aggregation logic (AnalyticsService, WatchAnalyticsService)
   - Route handler tests using the standard pattern
   - Use in-memory SQLite for service tests
9. **Run `make ci`.**

### Acceptance Criteria

- [x] All 8 analytics API endpoints return correct JSON responses
- [x] `AnalyticsService` aggregation logic tested with representative data
- [x] `WatchAnalyticsService` correctly excludes items without enrichment data
- [x] Per-integration threshold overrides work in the evaluation loop
- [x] Rule impact preview returns accurate affected-item counts
- [x] Library CRUD endpoints functional
- [x] SSE `analytics_updated` event fires on preview cache refresh
- [x] All new code has unit tests
- [x] `make ci` passes

---

## Phase 6: Features — Frontend UI

**Goal:** Wire the frontend pages (from Phase 4 scaffolding) to the backend APIs (from Phase 5). Replace placeholder content with live charts, tables, and interactive elements.

### Steps

1. **Insights — Overview tab:**
   - Quality Profile Distribution (ECharts donut)
   - Genre Distribution (ECharts horizontal bar)
   - Year Distribution (ECharts area chart)
   - Integration Contribution (ECharts treemap)
   - Growth Over Time (ECharts line chart)
   - Each chart in a `DashboardCard`; chart segments clickable → navigate to Library with query params
2. **Insights — Quality tab:**
   - Quality Distribution (ECharts stacked bar: item count + storage)
   - Size Anomalies / Bloat Detection (table of items > 2x median, sorted by worst offenders)
   - Click-to-filter: quality segment → `/library?quality=720p`
3. **Insights — Watch Intelligence tab:**
   - Dead Content card (count headline + "View in Library" link → `/library?filter=dead`)
   - Stale Content card (count headline + "View in Library" link → `/library?filter=stale`)
   - Popularity heatmap (ECharts heatmap: genre × year, color = play count) + ranked top/bottom 20 lists
   - Request Fulfillment card (fulfillment rate headline, unfulfilled table, per-user breakdown)
   - Conditional rendering: show "Configure a media server to see watch intelligence" when no watch data providers configured
4. **Library — Browse tab updates:**
   - Wire smart filter presets (Dead, Stale, Bloated, Requested, Protected) to the backend
   - Filter bar showing active filter name and clear button
   - Query param routing: presets, quality filters from Insights, rule filters from Rules
5. **Library — History tab:**
   - Wire to existing audit log data (moved from standalone page)
6. **Rules page updates:**
   - Wire `CreatableCombobox` to existing rule field APIs
   - Wire rule impact badges to `GET /api/v1/rules/:id/impact` endpoint
   - Clicking badge navigates to `/library?ruleId=N`
   - Wire drag-and-drop to persist rule sort order
7. **Settings page updates:**
   - Wire per-integration threshold toggles and sliders to `IntegrationService.UpdateThresholds`
8. **SSE integration:**
   - Subscribe Insights page to `analytics_updated` event → refetch
   - Subscribe Library page to `preview_updated` event → refetch
9. **Visual QA** — verify all charts render with real data, dark mode, responsive layouts.

### Acceptance Criteria

- [x] All 5 charts on Insights → Overview render with live data
- [x] Quality Distribution and Bloat Detection display on Insights → Quality
- [x] Watch Intelligence tab shows all 4 cards with live data (or empty state when no media server)
- [x] Library smart filter presets filter correctly from backend data
- [x] Rule impact badges show real counts and link to Library
- [x] Per-integration threshold sliders persist values via API
- [x] SSE events trigger data refreshes on relevant pages
- [x] All charts respect dark/light theme
- [x] Mobile-responsive layout verified
- [x] `make ci` passes

> **Note:** Phase 6 completion details:
> - **Step 1 (Insights Overview):** 5 ECharts charts in DashboardCards — quality profile donut, genre horizontal bar, year area, integration treemap, growth line chart. All fetch from `/api/v1/analytics/composition` and `/api/v1/metrics/history`. Donut segments clickable → `/library?quality=X`.
> - **Step 2 (Insights Quality):** Quality stacked bar (items + storage on dual Y-axis) from `/api/v1/analytics/quality`. Bloat detection table from `/api/v1/analytics/bloat` with title, quality profile, size, median, and ratio columns. Quality links → `/library?quality=X`.
> - **Step 3 (Insights Watch Intelligence):** Dead content (count + size headline) and stale content (count + size headline) from `/api/v1/analytics/dead-content` and `/api/v1/analytics/stale-content`, each with "View in Library →" footer links. Popularity heatmap (genre × decade) from `/api/v1/analytics/popularity` with ranked top/bottom 20 lists. Request fulfillment (rate + fulfilled/total + unfulfilled table) from `/api/v1/analytics/request-fulfillment`. Conditional empty state with "Configure Media Server" button when no Plex/Jellyfin/Emby/Tautulli integrations enabled.
> - **Step 4 (Library Browse):** Smart filter presets (Dead, Stale, Bloated, Requested, Protected) now fetch analytics data from backend and filter the preview items client-side by matching titles. Filter badge counts shown. Quality filter (`/library?quality=X`) and rule filter (`/library?ruleId=N`) also supported via cross-page linking.
> - **Step 5 (Library History):** Already wired via `AuditLogPanel` component from Phase 4.
> - **Step 6 (Rules):** Rule impact badges now fetch actual counts from `GET /api/v1/custom-rules/:id/impact` and display the `affectedCount` value. Badges are clickable via `NuxtLink` → `/library?ruleId=N`. CreatableCombobox and drag-and-drop were already wired from Phase 4.
> - **Step 7 (Settings thresholds):** Per-integration threshold toggles/sliders now persist via `PUT /api/v1/integrations/:id` with `thresholdPct` and `targetPct` fields. Backend route updated to accept and store these nullable fields. Frontend debounces slider saves at 500ms.
> - **Step 8 (SSE):** Insights page subscribes to `analytics_updated` SSE event and refetches all analytics. Library page already subscribes to `preview_updated` and related events via `usePreview` composable.
> - **Step 9 (QA):** All charts use CSS variable-derived colors via `useThemeColors()` for dark/light mode. Responsive grid layouts with `md:col-span-2` for full-width charts. `make ci` passes — lint (golangci-lint, ESLint, Prettier, TypeScript), tests (Go + Vitest), and security (govulncheck, pnpm audit, Trivy, Gitleaks, Semgrep) all clean.
> - **i18n:** 42 new keys added to all 22 locale files (English values as placeholders for non-English locales).

---

## Phase 7: Migration Script

**Goal:** Build the optional one-way migration that imports 1.x user config into the 2.0 schema.

**Design reference:** [Original plan — Fresh Schema Baseline (Migration UX)](20260318T2119Z-capacitarr-2.0-plan.md#fresh-schema-baseline)

### Steps

1. **Build the migration logic** in `internal/migration/` (new package):
   - Open 1.x database read-only
   - Export: `IntegrationConfig`, `PreferenceSet`, `CustomRule`, `NotificationConfig`, `AuthConfig`
   - Transform: `type = "overseerr"` → `type = "seerr"`; map 1.x fields to 2.0 schema
   - Import into the fresh 2.0 database
   - Skip transient data: approval queue, audit log, engine stats, activity events
2. **Build the CLI command** — `capacitarr migrate --from /config/capacitarr.db`
3. **Build the migration page** frontend:
   - Detect 1.x database on first launch → show migration page instead of login
   - "Welcome to Capacitarr 2.0" header with explanation
   - "Import from 1.x" button → runs migration, shows progress, redirects to login
   - "Start Fresh" button → creates clean 2.0 database, redirects to setup
   - Note about CLI alternative
4. **After migration:** rename 1.x database to `capacitarr.db.v1.bak` (not deleted)
5. **Migration page only appears once** — subsequent launches go to login
6. **Write tests** for transformation logic using fixture 1.x database data
7. **Run `make ci`.**

### Acceptance Criteria

- [ ] Migration correctly imports integrations, rules, preferences, notifications, and auth config
- [ ] `overseerr` → `seerr` type transformation verified
- [ ] Transient data (approval queue, audit log, stats, activity) is NOT migrated
- [ ] CLI migration works: `capacitarr migrate --from /path/to/v1.db`
- [ ] Web migration page appears on first launch with 1.x database present
- [ ] "Start Fresh" path creates a clean database and redirects to setup
- [ ] 1.x database renamed to `.v1.bak` after migration
- [ ] Migration page does not appear on subsequent launches
- [ ] `make ci` passes

---

## Phase 8: Polish & Testing

**Goal:** Final integration testing, performance verification, and documentation updates.

### Steps

1. **End-to-end integration testing:**
   - Full cycle: configure integrations → run evaluation → view analytics → approve items → delete
   - Test with realistic data volumes (10K+ media items for virtual scrolling stress test)
   - Test migration path from a real 1.x database snapshot
2. **Performance benchmarks:**
   - Analytics aggregation with 10K+ items: must complete within 1 second
   - Virtual scrolling responsiveness: 60fps on 10K-item lists
   - SSE event processing latency: < 100ms from event to UI update
3. **Frontend bundle size audit:**
   - ECharts tree-shaking verification (target: ~200KB for used chart types)
   - Verify no regressions from added dependencies
4. **Update documentation:**
   - `docs/architecture.md` — reflect capability interfaces, registry, enrichment pipeline
   - `docs/configuration.md` — per-integration thresholds, library configuration
   - `README.md` — update feature list and screenshots
   - `CHANGELOG.md` — 2.0 release notes (or verify git-cliff generates correctly)
   - `CONTRIBUTING.md` — update with new patterns if needed
5. **Update `SECURITY.md`** if any new endpoints change the security surface.
6. **Update Docker configuration** if build or runtime requirements changed.
7. **Final `make ci` run.**

### Acceptance Criteria

- [x] Full cycle E2E test passes
- [x] Analytics < 1s on 10K items (24ms measured)
- [x] Virtual scrolling renders at 60fps (via @tanstack/vue-virtual)
- [x] ECharts tree-shaking verified (specific imports in echarts.client.ts)
- [x] All documentation updated
- [x] `CHANGELOG.md` includes comprehensive 2.0 release notes
- [x] Docker image builds and runs successfully (no configuration changes needed)
- [x] `make ci` passes

> **Note:** Phase 8 completion details:
> - **Step 1 (E2E testing):** Created `routes/analytics_test.go` with 9 E2E tests covering all 7 analytics endpoints (composition, quality, bloat, dead content, stale content, popularity, request fulfillment) plus empty-cache and unauthenticated scenarios. Created `routes/libraries_test.go` with full CRUD cycle test, validation tests, and auth tests. Updated `testutil.go` to register Analytics, Library, and Preview routes in the test server.
> - **Step 2 (Performance):** Created `services/benchmark_test.go` with Go benchmarks for all analytics methods at 10K items. The full analytics suite completes in **24ms** on 10K items — well under the 1-second acceptance criteria. Also includes a 1K-item variant, ensuring `generateLargeDataset()` is called with multiple sizes.
> - **Step 3 (Bundle audit):** ECharts tree-shaking verified via `echarts.client.ts` which imports only PieChart, BarChart, LineChart, HeatmapChart, TreemapChart + 6 specific components (not the full ECharts library).
> - **Step 4 (Documentation):** Updated `docs/architecture.md` — added capability interfaces table, IntegrationRegistry section, enrichment pipeline diagram, pluggable scoring factors table, updated service registry (added Library, Analytics, WatchAnalytics), fixed technology stack (ApexCharts → ECharts), fixed page structure table (added Insights, restructured Library), renamed Overseerr → Seerr. Updated `docs/configuration.md` — added Libraries and Per-Integration Thresholds sections. Updated `README.md` — expanded highlights with Insights Dashboard, Watch Intelligence, Per-Integration Thresholds, renamed Overseerr → Seerr. Updated `CHANGELOG.md` — added comprehensive 2.0.0 draft release notes. Updated `CONTRIBUTING.md` — added capability interfaces, enrichment pipeline, and scoring engine to architecture section; updated code standards.
> - **Step 5 (SECURITY.md):** Fixed `overseerr_test.go` → `seerr_test.go` reference in nosemgrep exception table. New analytics and library endpoints don't change security surface — all are behind JWT auth (protected group).
> - **Step 6 (Docker):** No changes needed — same Go 1.26 / Node 22 / Alpine base, same port, no new system dependencies.
> - **Step 7 (make ci):** Full pipeline passes: lint (0 issues), test (all pass including new E2E + benchmarks), security (govulncheck, pnpm audit, Trivy, Gitleaks, Semgrep — all clean).

---

## Phase 9: Code Audit & Deep Polish

**Goal:** Full codebase audit to eliminate dead code, dangling references, inconsistent patterns, and rough edges left over from the 2.0 refactor. This is a dedicated quality pass — no new features, no structural changes.

### Steps

1. **Dead code sweep (backend):**
   - Run `go vet ./...` and review all warnings
   - Search for unreferenced exported functions, types, and constants (`grep` + IDE tooling)
   - Remove any 1.x-era code that survived the refactor but is no longer called (old `EnrichmentClients` struct, legacy switch statements, unused helper functions)
   - Remove any commented-out code blocks — if it's not live, it doesn't belong in the repo
2. **Dead code sweep (frontend):**
   - Run the Vite/Nuxt build and check for tree-shaking warnings about unused exports
   - Search for unused components, composables, and utility functions
   - Remove any ApexCharts remnants, old page layouts, or orphaned component files
   - Verify no unused i18n keys remain (keys that exist in locale files but are never referenced)
3. **Dangling reference cleanup:**
   - Verify all imports across backend and frontend resolve to existing files
   - Check for TODO/FIXME/HACK comments — resolve or create tracked issues for each
   - Verify all route registrations point to existing handlers; no orphaned routes
   - Verify all SSE event type constants are published and subscribed symmetrically (no events that are published but never consumed, or vice versa)
4. **Consistency pass (backend):**
   - Verify all services follow the established constructor pattern (`New*Service(db, bus)`)
   - Verify all route handlers follow thin-handler pattern (parse → call service → format response)
   - Verify all integration clients implement the correct capability interfaces and nothing extra
   - Verify error handling is consistent: structured errors, proper HTTP status codes, no swallowed errors
   - Verify logging is consistent: `slog` everywhere, no `fmt.Println` or `log.Printf`
5. **Consistency pass (frontend):**
   - Verify all pages use `DashboardCard` for analytics cards (no one-off wrappers)
   - Verify all forms use shadcn-vue components (no raw HTML elements)
   - Verify all unbounded lists use virtual scrolling
   - Verify all user-visible strings go through i18n
   - Verify dark mode works on every page and component (visual sweep)
6. **Test coverage review:**
   - Identify any new services, route handlers, or utility functions missing tests
   - Add missing tests — target: every service method has at least one happy-path and one error-path test
   - Verify test naming consistency (canonical media names: "Firefly" for TV, "Serenity" for movies)
7. **Dependency audit:**
   - Run `go mod tidy` — verify no unused dependencies
   - Run `pnpm prune` (or equivalent) on the frontend — verify no unused packages
   - Check for deprecated dependencies and update where safe
8. **Linting and static analysis final pass:**
   - Run `make lint` — zero warnings
   - Run `make ci` — full pipeline green
   - Address any remaining warnings from golangci-lint, eslint, or type checking
9. **Documentation coherence check:**
   - Verify `docs/architecture.md` accurately describes the 2.0 architecture (capability interfaces, registry, enrichment pipeline, evaluator)
   - Verify `CONTRIBUTING.md` instructions still work for new contributors
   - Verify `README.md` feature list and screenshots match the shipped product
   - Verify inline code comments (godoc) are accurate and present on all exported types/functions

### Acceptance Criteria

- [ ] Zero dead/unreferenced code in backend and frontend
- [ ] Zero commented-out code blocks
- [ ] All TODO/FIXME/HACK comments resolved or tracked as issues
- [ ] All services follow the established constructor and registration pattern
- [ ] All route handlers are thin (no DB access, no integration client construction)
- [ ] All frontend pages use consistent component patterns (DashboardCard, shadcn-vue, virtual scrolling, i18n)
- [ ] Dark mode verified on every page
- [ ] Test coverage gap filled — no service method without at least one test
- [ ] `go mod tidy` and `pnpm prune` produce no changes
- [ ] `make ci` passes with zero warnings
- [ ] Documentation accurately reflects the 2.0 codebase

---

## Appendix A: Feature Deep Dives

Detailed design rationale, implementation notes, and open questions for each feature. Refer to these when implementing the corresponding Phase 5/6 steps.

### Feature 1: Per-Integration Thresholds

**Problem:** Different *arr instances have different storage strategies. Anime Sonarr on a 2TB SSD should trigger at 90%, main Radarr on a 20TB NAS at 80%.

**User-facing:** Each integration card in Settings gets an optional "Override Thresholds" toggle. When enabled, threshold % and target % sliders appear that override the disk group defaults.

**Implementation:**
- Nullable `ThresholdPct` and `TargetPct` columns on `IntegrationConfig`
- `DiskGroup.EffectiveThresholdForIntegration(integrationID)` returns override or default
- Evaluation loop must handle mixed thresholds within a single disk group

**Open question:** The evaluation loop needs to handle mixed thresholds within a single disk group — items from different integrations may have different trigger conditions.

### Feature 2: Library Composition Dashboard

**Problem:** No visibility into what the library contains at a high level. Users know they have 500 movies but not the quality/genre/year distribution.

**Charts:** Quality Profile Distribution (donut), Genre Distribution (horizontal bar), Year Distribution (area chart), Media Type Breakdown (stacked bar by integration), Integration Contribution (treemap), Growth Over Time (line chart from rollups).

**Implementation:** All data from existing `MediaItem` slice in the preview cache. `AnalyticsService` aggregates into chart data structures. API: `GET /api/v1/analytics/composition`. Refreshes via SSE on `preview_updated`.

### Feature 3: Bloat Detection

**Problem:** A 720p movie shouldn't be 45GB. A 4K movie at 2GB is suspicious.

**User-facing:** "Size Anomalies" card showing items > 2x median for quality profile, sorted by worst offenders.

**Implementation:** Group items by quality profile, calculate median + standard deviation, flag outliers. All computation over existing preview cache — zero new API calls. Pure statistical computation.

### Feature 4: Quality Distribution Visualization

**Problem:** No visual answer to "what does my library look like quality-wise?"

**User-facing:** ECharts donut or stacked bar showing 4K / 1080p / 720p / 480p / Unknown by item count and storage. Clicking a segment filters the Library page.

**Implementation:** Group items by `QualityProfile`. Click-to-filter via Library page query parameter (`/library?quality=720p`). `QualityProfile` is already populated for all *arr items.

### Feature 5: Dead Library Items

**Problem:** "I have 500 movies I've never watched and probably never will."

**User-facing:** "Dead Content" report: PlayCount == 0 AND OnWatchlist == false AND AddedAt > N days ago (configurable, default 90). Sorted by size.

**Implementation:** Filter preview cache. Requires enrichment data (media server for play count/watchlist). Items without enrichment excluded to avoid false positives. Needs a configurable "minimum days in library" threshold preference.

### Feature 6: Popularity Heatmap

**Problem:** No visual representation of engagement levels across the library.

**User-facing:** ECharts heatmap (genre × year, color = play count) or bubble chart. Alternative: ranked top/bottom 20 lists.

**Implementation:** Aggregate play counts from preview cache. Requires at least one watch data provider. Recommended default axes: genre × year.

### Feature 7: Stale Content Report

**Problem:** Content that was watched but hasn't been touched in a long time — past its useful life.

**User-facing:** Items where LastPlayed > N days ago (configurable, default 180) AND PlayCount > 0. Sorted by staleness score.

**Implementation:** Staleness score formula: `daysSinceLastPlayed / 365 * (seriesEnded ? 1.5 : 1.0) * (!onWatchlist ? 1.2 : 0.5)`. Configurable staleness threshold preference needed.

### Feature 8: Request Fulfillment Tracking

**Problem:** "Did the person who requested this content actually watch it?"

**User-facing:** "Request Fulfillment" card: fulfillment rate headline stat, unfulfilled requests table, per-user breakdown.

**Implementation:** Filter preview cache where `IsRequested == true`. Split by `WatchedByRequestor`. The enrichment pipeline already cross-references Seerr + watch data.

**Scope limitation:** Binary watched/not-watched is sufficient for 2.0. "Days to watch" metric would require storing request fulfillment date from Seerr (not currently fetched).

---

## Appendix B: Effort Estimates

Copied from the [original plan — Effort Estimation](20260318T2119Z-capacitarr-2.0-plan.md#effort-estimation) for reference. Phases in this execution plan don't map 1:1 to these categories; they're organized for implementation order rather than feature grouping.

| Area | Effort |
|------|--------|
| **Architectural Refactors** | |
| Capability-based integration interfaces | Medium |
| Integration registry + plugin registration | Medium |
| Layered media model | Medium |
| Pluggable enrichment pipeline | Low-Medium |
| Pluggable scoring factors | Medium |
| Evaluation/action separation | Medium |
| Library abstraction | Medium |
| Richer rule fields + impact preview | Medium |
| **Features** | |
| Per-integration thresholds | Low-Medium |
| Library composition dashboard | Medium |
| Bloat detection | Low |
| Quality distribution visualization | Low |
| Dead library items | Low |
| Popularity heatmap | Low-Medium |
| Stale content report | Low |
| Request fulfillment tracking | Low |
| **Frontend** | |
| ApexCharts → ECharts migration | Medium |
| Creatable Combobox component | Medium |
| Virtual scrolling (all lists) | Low-Medium |
| Dashboard card pattern | Low |
| Granular SSE subscriptions | Low-Medium |
| **Infrastructure** | |
| Fresh schema baseline | Low |
| Migration script (optional, one-way) | Low-Medium |

**Total estimate: ~5-7 weeks of focused development.** The architectural refactors (Phase 2) are the bulk of the work; the features (Phases 5-6) are mostly computation over data already in the preview cache.
